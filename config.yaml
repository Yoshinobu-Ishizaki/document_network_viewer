categories:
  - Jigs & Tools
  - Manufacturing
  - Programming
  - Meeting & Business Trip
  - Other Stuff

# Subcategory constraints (applied after LLM categorization).
# Set to null or remove the block to disable constraint enforcement.
subcategory_constraints:
  max_subcategories: 8        # hard upper limit on L2 count per L1 category
  min_docs_per_subcategory: 2 # merge L2s smaller than this into the nearest neighbor
